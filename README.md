# ABYSS

## Run it
npm install
npm run dev

Then open the localhost URL it prints.

## Next steps
- Drop stock footage into public/video/ named exactly:
  01-surface.mp4, 02-dive.mp4, 03-reef.mp4, 04-bioluminescence.mp4,
  05-ruins.mp4, 06-darkness.mp4, 07-abyss.mp4
- Drop matching poster images into public/img/ (same names, -poster.jpg)
- Scroll speed per scene = the `height` prop on each ScrollVideoController
  (taller = slower/finer scrub)
